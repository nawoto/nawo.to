---
import type { StructuredDataProps } from '../types/index.js';

interface Props extends StructuredDataProps {}

const { data } = Astro.props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify(data)} /> 